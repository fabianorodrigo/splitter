const Splitter = artifacts.require('Splitter');

contract('Splitter', (accounts) => {
    it('should deploy the contract with 3 different addresses', async () => {
        // Set up 3 test accounts
        account1 = accounts[0];
        account2 = accounts[1];
        account3 = accounts[2];
    })
})